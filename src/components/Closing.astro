---
import { UNDANGAN_CONFIG } from '../data/config'
import OrnamentBackground from './OrnamentBackground.astro'
const { brideFullName, groomFullName } = UNDANGAN_CONFIG
const sponsorImages = UNDANGAN_CONFIG.sponsors

import OrnamentBackground from './OrnamentBackground.astro'
---

<div
  id="closing"
  class="angkasa_slide relative flex snap-start items-center justify-center overflow-hidden"
  data-section="closing"
>
  <OrnamentBackground />

  <div
    class="angkasa_inner flex h-[calc(100dvh-80px)] w-full flex-col justify-center px-6 py-6 pt-10 sm:px-6 sm:py-8"
  >
    <div
      class="blur-card animated-content flex h-full w-full max-w-[540px] flex-col justify-center rounded-3xl bg-(--inv-bg)/30 p-6 shadow-lg backdrop-blur-sm sm:p-8"
    >
      <div
        class="content-layer font-dekor-1 mx-auto w-full max-w-[420px] px-4 py-4 text-center sm:px-6"
      >
        <!-- Judul -->
        <h3
          data-animation-class="animate__fadeInDown"
          class="ornament-item animate__slow font-judul color-accent-2 border-rose-gold mb-4 inline-block border-b pb-2 text-2xl italic sm:text-3xl"
        >
          ✤ Terimakasih ✤
        </h3>

        <!-- Paragraf -->
        <p
          data-animation-class="animate__zoomIn"
          class="ornament-item animate__slower text-[15px] leading-relaxed"
        >
          Merupakan suatu kehormatan dan kebahagiaan yang tak terhingga bagi
          kami, apabila Bapak/Ibu, Saudara/i berkenan hadir untuk memberikan
          do'a restu.
        </p>

        <p
          data-animation-class="animate__zoomIn"
          class="ornament-item animate__slower text-[15px] leading-relaxed"
        >
          Semoga Allah SWT senantiasa melimpahkan rahmat-Nya kepada kita semua.
        </p>

        <!-- Nama Mempelai -->
        <div
          data-animation-class="animate__zoomIn"
          class="ornament-item animate__slower animate__delay-1s mt-6"
        >
          <p class="font-judul color-accent mb-4 text-lg italic">
            Kami yang berbahagia
          </p>

          <!-- 3 Kolom: Bride | Heart | Groom -->
          <div
            class="mx-auto grid w-full max-w-[420px] grid-cols-[1fr_auto_1fr] items-center gap-4 text-center"
          >
            <!-- Bride -->
            <div class="flex flex-col">
              <h3
                class="font-dekor-2 color-accent text-xl font-semibold tracking-wide whitespace-nowrap sm:text-2xl"
              >
                {brideFullName}
              </h3>
            </div>

            <!-- Middle Heart -->
            <div class="flex items-center justify-center px-1">
              <i class="fa-solid fa-heart color-accent text-[20px]"></i>
            </div>

            <!-- Groom -->
            <div class="flex flex-col">
              <h3
                class="font-dekor-2 color-accent text-xl font-semibold tracking-wide whitespace-nowrap sm:text-2xl"
              >
                {groomFullName}
              </h3>
            </div>
          </div>

          <p class="font-base mt-4 text-sm italic opacity-80">
            (Beserta Keluarga Besar)
          </p>
        </div>

        <!-- Sponsor -->
        <div
          data-animation-class="animate__fadeInUp"
          class="ornament-item animate__slower animate__delay1s mt-12 pt-10 text-center"
        >
          <!-- LABEL -->
          <p
            class="text-color-base mb-6 text-[12px] tracking-widest uppercase opacity-70"
          >
            Supported by
          </p>

          <!-- GRID LOGO -->
          <div
            id="sponsor-container"
            class="grid w-full grid-cols-2 justify-items-center gap-4 sm:grid-cols-2"
          >
            {
              sponsorImages.map((imagePath) => (
                <div class="flex items-center justify-center rounded-xl border border-white/30 bg-white/20 p-2 shadow-sm backdrop-blur-sm transition-all hover:bg-white/30">
                  <img
                    src={imagePath}
                    alt="Sponsor Logo"
                    class="max-h-[40px] w-auto opacity-90 sm:max-h-[50px]"
                    loading="lazy"
                  />
                </div>
              ))
            }
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
